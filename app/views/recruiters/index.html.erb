<p id="notice"><%= notice %></p>

<h1>Listing Recruiters</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Email</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @recruiters.each do |recruiter| %>
      <tr>
        <td><%= recruiter.first_name %></td>
        <td><%= recruiter.last_name %></td>
        <td><%= recruiter.email %></td>
        <td><%= link_to 'Show', recruiter %></td>
        <td><%= link_to 'Edit', edit_recruiter_path(recruiter) %></td>
        <td><%= link_to 'Destroy', recruiter, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% unless user_signed_in? %>
          <td><%= link_to 'Sign In', sessions_create_path(id: recruiter.id, klass: Recruiter), data: { method: 'post' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recruiter', new_recruiter_path %>
