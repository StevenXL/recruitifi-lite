<%= form_for @question do |f| %>
  <%= if @question.errors.any? %>
    <p><%= pluralize(@question.errors.count, "error")%> prevented this form from being saved</p>
    <ul>
      <%= @question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
    </ul>
  <% end %>
  <%= f.hidden_field :job_id %>

  <p><%= f.text_field :body, placeholder: "Your Question Here" %></p>

  <p><%= f.submit %></p>
<% end %>
